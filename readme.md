This is a simple Java countdown application.

It will write the output of a counter to the file, countdown.txt.

It can currently accept a timer based on upcoming HOUR and MINUTE, dates are not implemented.